#Set up API call

from urllib import urlopen

prefix = "http://search.twitter.com/search.json"
suffix = "?"+ urlencode({"q":"#thfcvafc","rpp":100})

url = prefix+suffix

data = urlopen(url).read()
response = loads(data)
tweets = response["results"]
